import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6941C6",
      light: "#CAB4FF",
    },
    secondary: {
      main: "#FFF",
    },
  },
});

export default theme;
