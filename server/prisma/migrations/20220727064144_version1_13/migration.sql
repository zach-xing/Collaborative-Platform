/*
  Warnings:

  - You are about to drop the column `onlineUserIds` on the `collaborator` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `collaborator` DROP COLUMN `onlineUserIds`;
